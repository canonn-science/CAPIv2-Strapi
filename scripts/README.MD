Use the following script `downloadExampleData.sh` to grab current example files from Google Sheets in CSV Format. They will be placed in `seed_files/CSV/exampleData/` and will overwrite current ones.
