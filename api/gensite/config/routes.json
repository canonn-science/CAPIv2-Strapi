{
  "routes": [
    {
      "method": "GET",
      "path": "/gensites",
      "handler": "Gensite.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/gensites/count",
      "handler": "Gensite.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/gensites/:id",
      "handler": "Gensite.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/gensites",
      "handler": "Gensite.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/gensites/:id",
      "handler": "Gensite.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/gensites/:id",
      "handler": "Gensite.destroy",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/gensites/:id/relationships/:relation",
      "handler": "Gensite.createRelation",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/gensites/:id/relationships/:relation",
      "handler": "Gensite.updateRelation",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/gensites/:id/relationships/:relation",
      "handler": "Gensite.destroyRelation",
      "config": {
        "policies": []
      }
    }
  ]
}
