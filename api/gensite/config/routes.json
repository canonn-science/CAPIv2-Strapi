{
  "routes": [
    {
      "method": "GET",
      "path": "/gensites",
      "handler": "Gensite.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/gensites/count",
      "handler": "Gensite.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/gensites/:id",
      "handler": "Gensite.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/gensites",
      "handler": "Gensite.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/gensites/:id",
      "handler": "Gensite.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/gensites/:id",
      "handler": "Gensite.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
