{
  "routes": [
    {
      "method": "GET",
      "path": "/info",
      "handler": "global.info",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/version",
      "handler": "global.version",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/totalcount",
      "handler": "global.totalCount",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/stats",
      "handler": "global.stats",
      "config": {
        "policies": []
      }
    }
  ]
}
