dist: bionic
language: node_js
services:
  - mysql
addons:
  mariadb: '10.0'
  apt:
    packages:
      - software-properties-common
      - build-essential
node_js:
  - "10"

install:
  - npm install

script:
  - pm2 start strapi --no-pmx --name="capiv2" -- start
  - pm2 stop capiv2
